import { MatMultiSortDirective } from "./mat-multi-sort.directive";

describe("MatMultiSortDirective", () => {
  it("should create an instance", () => {
    const directive = new MatMultiSortDirective();
    expect(directive).toBeTruthy();
  });
});
